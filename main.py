from frontend.main_window import main

if __name__ =="__main__":
    main()
    # main() is the entry point of the application, which starts the GUI and handles user interactions.
    # It is defined in the frontend.main_window module.
    # The if __name__ == "__main__": block ensures that this code runs only when the script is executed






